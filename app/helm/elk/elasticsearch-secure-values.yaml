replicas: 1
minimumMasterNodes: 1

esJavaOpts: "-Xms512m -Xmx512m"

resources:
  requests:
    cpu: "250m"
    memory: "1Gi"
  limits:
    cpu: "500m"
    memory: "2Gi"

persistence:
  enabled: false

xpack:
  security:
    enabled: true
  monitoring:
    collection:
      enabled: true
